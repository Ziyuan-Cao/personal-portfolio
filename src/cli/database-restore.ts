import fs from "node:fs";import path from "node:path";import { loadConfig } from "../infrastructure/config.js";
const source=process.argv[2];if(!source)throw new Error("Usage: npm run db:restore -- <backup-file>");const resolved=path.resolve(source);if(!fs.existsSync(resolved))throw new Error(`Backup does not exist: ${resolved}`);const header=Buffer.alloc(16);const handle=fs.openSync(resolved,"r");fs.readSync(handle,header);fs.closeSync(handle);if(header.toString()!=="SQLite format 3\u0000")throw new Error("Backup is not a SQLite database");const target=loadConfig().databasePath;fs.mkdirSync(path.dirname(target),{recursive:true});if(fs.existsSync(target))fs.copyFileSync(target,`${target}.before-restore-${Date.now()}`);fs.copyFileSync(resolved,target);console.log(target);
