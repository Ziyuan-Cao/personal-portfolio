{
  "name": "personal-portfolio-information-hub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "node --import tsx --watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "collect:static": "npm run build && node dist/static-collector.js",
    "start": "node dist/server.js",
    "test": "npm run build"
  },
  "dependencies": {
    "@fastify/static": "^10.1.3",
    "cheerio": "^1.1.2",
    "fast-xml-parser": "^5.2.5",
    "fastify": "^5.6.2"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
